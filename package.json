{
	"name": "kaholo-plugin-aws-code-deploy",
	"version": "1.0.0",
	"description": "Kaholo plugin for integration with AWS Code Deploy API.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kaholo Group(Amit Alon)",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.1021.0"
	}
}
